grumphp:
    
    tasks:
        phpcs:
            standard: ./phpcs.xml
        phpstan:
            configuration: ./phpstan.neon
            level: 5
        phpmd:
            whitelist_patterns:
                - 'src'
            ruleset:
                - 'phpmd.xml'
    fixer:
        enabled: true
        fix_by_default: true
